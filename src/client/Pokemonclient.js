function obtenerAleatorio(min,max){
    return Math.floor((Math.random() * (max - min + 1)) + min);
}

const obtenerArregloNumerico=()=>{
    const arreglo=[];
    for(let i=0; i<4; i++){

        arreglo[i] = obtenerAleatorio {1, 600};
    }
    return arreglo;
}

const obtenerArreglosPokemons=(arreglo)=>{
    const arregloPokemon=[];
    const obj1={
        
    };
    const obj2={
        
    };
    const obj3={
        
    };
    const obj4={
        
    };

    arregloPokemon[0]=obj1;
    arregloPokemon[1]=obj2;
    arregloPokemon[2]=obj3;
    arregloPokemon[3]=obj4;
}